services: 
  hoodsbase:
    build: .
    container_name: hoodsbase-dev
    env_file: .env
    volumes:
      - .:/app          # mount entire project — code changes apply without rebuild
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped 
