# SafeHoods

Business intelligence system for a hood cleaning company. Data synced from ServiceTrade API into SQLite.

The MCP server (`mcp/server.py`) provides database access. The server's `instructions` field contains the full annotated schema — it is delivered automatically when a client connects.

For project architecture, see `docs/architecture.md`.
